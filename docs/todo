env -i

write qui echoue

export qui redirect pas

exit dans sortie erreur

print buffer

exit long overflow

prompt qui saute a la ligne au mauvais endroit

execve gestion d'erreur (fichier, dossier, pas de permission)

exit doit garder le dernier g_status

si atol plus grand que long max alors renvoyer une erreur (il ne le considere plus comme un nombre mais comme une chaine de caractere)